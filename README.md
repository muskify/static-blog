# Development
```
docker-compose up
```
```
docker-compose down
```
Live on ```http://0.0.0.0:4000/```. Remember if you modify ```_config.yml``` you have to restart to reflect the changes

# Deployment
Master branch always get deployed if the commit pass the Jekyll tests